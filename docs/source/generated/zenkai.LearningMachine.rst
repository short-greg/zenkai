zenkai.LearningMachine
======================

.. currentmodule:: zenkai

.. autoclass:: LearningMachine

   
   .. automethod:: __init__

   
   .. rubric:: Methods

   .. autosummary::
   
      ~LearningMachine.__init__
      ~LearningMachine.accumulate
      ~LearningMachine.accumulate_posthook
      ~LearningMachine.add_module
      ~LearningMachine.apply
      ~LearningMachine.assess
      ~LearningMachine.assess_y
      ~LearningMachine.bfloat16
      ~LearningMachine.buffers
      ~LearningMachine.children
      ~LearningMachine.cpu
      ~LearningMachine.cuda
      ~LearningMachine.double
      ~LearningMachine.eval
      ~LearningMachine.extra_repr
      ~LearningMachine.float
      ~LearningMachine.forward
      ~LearningMachine.forward_hook
      ~LearningMachine.forward_io
      ~LearningMachine.forward_nn
      ~LearningMachine.get_buffer
      ~LearningMachine.get_extra_state
      ~LearningMachine.get_parameter
      ~LearningMachine.get_submodule
      ~LearningMachine.half
      ~LearningMachine.learn
      ~LearningMachine.learner_hook
      ~LearningMachine.lmode_
      ~LearningMachine.load_state_dict
      ~LearningMachine.modules
      ~LearningMachine.named_buffers
      ~LearningMachine.named_children
      ~LearningMachine.named_modules
      ~LearningMachine.named_parameters
      ~LearningMachine.parameters
      ~LearningMachine.register_backward_hook
      ~LearningMachine.register_buffer
      ~LearningMachine.register_forward_hook
      ~LearningMachine.register_forward_pre_hook
      ~LearningMachine.register_full_backward_hook
      ~LearningMachine.register_module
      ~LearningMachine.register_parameter
      ~LearningMachine.requires_grad_
      ~LearningMachine.set_extra_state
      ~LearningMachine.share_memory
      ~LearningMachine.state_dict
      ~LearningMachine.step
      ~LearningMachine.step_posthook
      ~LearningMachine.step_x
      ~LearningMachine.step_x_hook
      ~LearningMachine.test
      ~LearningMachine.to
      ~LearningMachine.to_empty
      ~LearningMachine.train
      ~LearningMachine.type
      ~LearningMachine.xpu
      ~LearningMachine.zero_grad
   
   

   
   
   .. rubric:: Attributes

   .. autosummary::
   
      ~LearningMachine.T_destination
      ~LearningMachine.dump_patches
      ~LearningMachine.lmode
      ~LearningMachine.training
   
   